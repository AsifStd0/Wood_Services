import 'package:flutter/material.dart';
import 'app_colors.dart';

class AppIcons {
  static Icon email = Icon(
    Icons.email,
    color: AppColors.black.withAlpha(250),
    size: 22,
  );

  static Icon password = Icon(
    Icons.lock,
    color: AppColors.black.withAlpha(250),
    size: 22,
  );

  static Icon phone = Icon(
    Icons.phone,
    color: AppColors.black.withAlpha(250),
    size: 22,
  );

  static Icon user = Icon(
    Icons.person,
    color: AppColors.black.withAlpha(250),
    size: 22,
  );
  static Icon lock = Icon(
    Icons.lock,
    color: AppColors.black.withAlpha(250),
    size: 22,
  );
  static Icon person = Icon(
    Icons.person,
    color: AppColors.black.withAlpha(250),
    size: 22,
  );
}
